spring:
  application:
    name: finance-tracker
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/finance_tracker?createDatabaseIfNotExist=true}
    driver-class-name: ${DATABASE_DRIVER:com.mysql.cj.jdbc.Driver}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:Raja2004@}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: ${HIBERNATE_DIALECT:org.hibernate.dialect.MySQLDialect}
  h2:
    console:
      enabled: true

# Security Configuration
security:
  jwt:
    secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970} # 256-bit key
    expiration: 86400000 # 24 hours

# Google OAuth
google:
  client-id: ${GOOGLE_CLIENT_ID:your-google-client-id}

server:
  port: 8080
